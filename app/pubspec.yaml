name: fish_alarm
description: FishAlarm BLE controller

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_blue_plus: ^1.32.0   # BLE
  provider: ^6.1.2              # state management
  flutter_local_notifications: ^17.2.2  # notifikácie pri zábere
  wakelock_plus: ^1.2.8         # drží displej hore pri alarme

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
